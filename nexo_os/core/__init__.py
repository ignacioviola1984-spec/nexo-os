"""Deterministic metric library — the only place a figure is ever produced.

Pure functions over typed domain objects. No model calls, no I/O beyond what the
repository provides. All currency math uses Decimal.
"""
